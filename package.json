{
  "name": "edusafe",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=22.9"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run --passWithNoTests",
    "db:migrate:badges": "node --env-file-if-exists=.env.local scripts/migrate-badges.mjs",
    "eas:register-schema": "node --env-file-if-exists=.env.local scripts/register-eas-schema.mjs",
    "deploy": "gh-pages -d dist",
    "sync:team": "git push team main:main"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.65.0",
    "ethers": "6.17.0",
    "pg": "8.16.3",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "gh-pages": "^6.3.0",
    "vite": "^6.2.0",
    "vitest": "^4.1.11"
  }
}
